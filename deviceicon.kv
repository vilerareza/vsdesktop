<DeviceIcon>:
    statusImage : status_image
    deviceLabel : device_label

    Image:
        id : status_image
        pos_hint : {'center_x':0.5, 'center_y': 0.5}
        source : "images/connecting.png"
    Label:
        id : device_label
        text : ""
        font_size : 18
        font_family : "arial" 
        halign : 'center'
        valign : 'middle'
        size_hint : (None, None)
        size : (120,40) 
        pos_hint : {'center_x':0.5, 'center_y': 0.5}
        markup : True
    #canvas.before:
    #    Color:
    #        rgb : (0,0,0.9)
    #    Rectangle:
    #        size : self.size
    #        pos : self.pos