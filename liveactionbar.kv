<LiveActionBar>:
    rows : 1
    opacity : 0.7
    canvas.before :
        Color:
            rgb : (0, 0, 0)
        Rectangle:
            size : self.size
            pos : self.pos
    
    BoxLayout :
        id : capture_image_box
        ImageButton:
            id : capture_image_button
            source : 'images/capturenormal.png'
            size_hint : (1, 0.8)
            pos_hint : {'center_x' : 0.5, 'center_y' : 0.5}
            on_press : root.button_press_callback(self)
            on_release : root.button_release_callback(self)
    
    BoxLayout:
        id : remove_livebox_box
        ImageButton:
            id : remove_livebox_icon
            source : 'images/removelivebox.png'
            size_hint : (1, 0.5)
            pos_hint : {'center_x' : 0.5, 'center_y' : 0.5}

<ImageButton@ButtonBehavior+Image>: